#include <stdio.h>

void swap(int* a, int* b);
int main(void)
{
    int tcase, arr[1001] = { 0 };
    scanf("%d", &tcase);
    for (int i = 0; i < tcase; i++) {
        scanf("%d", &arr[i]);
    }
    for (int i = 0; i < tcase - 1; i++) {
        for (int j = 0; j < tcase - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                swap(&arr[j], &arr[j + 1]);
            }
        }
    }
    for (int i = 0; i < tcase; i++) {
        printf("%d\n", arr[i]);
    }

    return 0;
}

void swap(int* a, int* b) {
    int temp = (*b);
    (*b) = (*a);
    (*a) = temp;
}


// 20.02.04. 백준 2750 '수 정렬하기 aka. 버블 정렬'
